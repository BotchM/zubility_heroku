extends layout

block content
  script(src= './js/' + jf)
  
html(lang='en')
  head
    meta(charset='UTF-8')
    title chat.io
    style.
      body { background: #e4f1fd; color: #1e233b; text-align: center; padding-top: 10vh; }
      h1, h2 {font-family: Poppins, Helvetica, Lato, Arial, sans-serif; font-weight: bold;}
      h1 { font-size: calc(20px + 5vw); }
      h2 { font-size: calc(16px + 3vw); letter-spacing: 1vw; color: #3e7ce4; }
  body
    h1 Zubility
    h2  / COMING SOON /
  
